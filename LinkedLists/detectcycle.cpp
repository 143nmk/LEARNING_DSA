/* Cpp code to detect the cycle in a linked list */

#include <iostream>
#include <list>
using namespace std;

bool detectCycle(list<int> head)
{
    
}

int main()
{
    list<int> ll;
    ll.push_back(558);
    ll.push_back(521);
    ll.push_back(523);
    list<int>::iterator it;
    // for (it = ll.begin(); it != ll.end(); it++)
    // {
    //     cout << (*it) << " ";
    // }
    return 0;
}